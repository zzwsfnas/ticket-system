# ============================================================
# 私有镜像仓库配置 —— 复制本文件为 .env 并填写实际值
# ============================================================

# 镜像完整地址（必填）。格式：<仓库地址>/<命名空间>/<镜像名>:<标签>
# 常见示例：
#   Aliyun ACR : registry.cn-hangzhou.aliyuncs.com/your_ns/ticket-system:latest
#   腾讯 TCR   : ccr.ccs.tencentyun.com/your_ns/ticket-system:latest
#   Harbor     : harbor.your.domain.com/ticket-system:latest
#   自建仓库   : 192.168.1.10:5000/ticket-system:latest
IMAGE=registry.example.com/ticket-system:latest

# 以下仅在私有仓库需要账号登录时填写（留空则跳过登录）
REGISTRY=registry.example.com
REGISTRY_USER=
REGISTRY_PASS=

# 可选：构建时同时推送一个版本标签（如 1.2.0），便于精确回滚
VERSION=

# ============================================================
# 方案 B：GitHub Container Registry (GHCR) —— 推荐
# 将源码推到 GitHub 后，GitHub Actions 自动构建并推送镜像到 GHCR，
# FNOS 上直接用 docker-compose.ghcr.yml 拉取，无需本机构建。
# 镜像地址格式固定为：ghcr.io/<你的GitHub用户名>/ticket-system:latest
# 例：IMAGE=ghcr.io/zhangsan/ticket-system:latest
# 注意：GHCR 镜像默认私有，FNOS 拉取前需二选一：
#   ① 在 GitHub 仓库 Settings → Packages → 该镜像 → Change visibility 设为 Public；
#   ② 或在 FNOS 上 `docker login ghcr.io -u 用户名 -p <具有 read:packages 的 token>` 后拉取。
# ============================================================

